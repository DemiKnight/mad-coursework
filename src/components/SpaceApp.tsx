import React from 'react';
import {SafeAreaView} from 'react-native';
import {Text} from 'react-native-elements';

export const SpaceApp = () => {
  return (
    <SafeAreaView>
      <Text>Bulk application</Text>
    </SafeAreaView>
  );
};
