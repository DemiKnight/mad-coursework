import React from 'react';
import {SafeAreaView} from 'react-native';
import {Text} from 'react-native-elements';

export const EditableProfile = () => {
  return (
    <SafeAreaView>
      <Text>Editable Profile</Text>
    </SafeAreaView>
  );
};
