import 'react-native';

describe('Login component', () => {
  it.todo('Renders');
  // TOOD Fix this
  // it('Renders properly', () => {
  //   renderer.create(<Login navigation={} route={} />);
  // });
});
